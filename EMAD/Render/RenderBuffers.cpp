#include "RenderBuffers.h"
#include <Graphics/Texture/GPUTexture.h>

RenderBuffers::RenderBuffers()
{
	//GPUSampler::ptr tempSampler = GPUSampler::Gen2D(GL_REPEAT, GL_REPEAT, GL_NEAREST, GL_NEAREST);
	//GPUTexDesc::ptr baseColorTexDesc = GPUTexDesc::Gen2D(0, GL_RGB, )
}
